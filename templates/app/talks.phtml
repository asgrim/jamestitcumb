<?php

declare(strict_types=1);

use Asgrim\Value\Talk;

/** @var Talk[] $upcoming */
/** @var Talk[] $past */

$this->headTitle()->append('Upcoming talks');
$this->headScript()->appendFile('//ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js');
?>

<p>Upcoming talks:</p>

<?php
if (count($upcoming)) {
    echo '<ul>';
    foreach ($upcoming as $t) {
        echo $this->renderTalk($t);
    }
    echo '</ul>';
} else {
    echo '<p>No upcoming talks...</p>';
}
?>
<hr />
<p>I have given <?= count($past); ?> in the past:</p>

<?php
echo '<ul>';
foreach ($past as $t) {
    echo $this->renderTalk($t, skipAbstract: true);
}
echo '</ul>';
?>
